name: flutter_data_setup_app
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.3
  flutter:
    sdk: flutter
  flutter_data: ^1.4.0
  flutter_riverpod: ^1.0.0
  get_it: ^7.2.0
  json_annotation: ^4.5.0
  path_provider: ^2.0.2
  provider: ^6.0.3

# dependency_overrides:
#   flutter_data:
#     path: ../flutter_data
dev_dependencies:
  build_runner: ^2.0.4
  flutter_test:
    sdk: flutter
  json_serializable: ^6.0.1

flutter:
  uses-material-design: true
